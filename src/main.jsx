import React from 'react'
import ReactDOM from 'react-dom/client'
import MetaphorSystem from './MetaphorSystem'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <MetaphorSystem />
  </React.StrictMode>
)
